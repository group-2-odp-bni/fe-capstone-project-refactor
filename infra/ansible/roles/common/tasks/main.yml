- name: Update apt cache
  apt:
    update_cache: yes

- name: Install base utilities
  apt:
    name:
      - curl
      - unzip
      - rsync
      - ufw
      - python3
      - python3-pip
    state: present
